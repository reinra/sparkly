// Mode enum shared between backend and frontend
// This represents the operating mode of Twinkly devices
export enum Mode {
  off = 'off',
  demo = 'demo',
  effect = 'effect',
  movie = 'movie',
  rt = 'rt',
}
